# Configuration API
ANTHROPIC_API_KEY=sk-ant-xxx  # Clé API Anthropic pour la recherche naturelle avec IA (optionnel)
OPENAI_API_KEY=sk-xxx          # Clé API OpenAI pour le chatbot

# Base de données
# IMPORTANT: Si le mot de passe ou le nom d'utilisateur contient des caractères spéciaux (é, è, @, #, etc.),
# utilisez l'encodage URL. Par exemple: é devient %C3%A9, @ devient %40
# Exemple: postgresql+psycopg2://user:mot%20de%20passe@localhost:5432/db_name
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/db_name

# Redis
REDIS_URL=redis://localhost:6379

# Elasticsearch
ELASTICSEARCH_URL=http://localhost:9200

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# JWT
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
